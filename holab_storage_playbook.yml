---
# ZFS RAIDZ1 — DESTRUCTIVE. Requires storage_confirm_destroy: true

- name: Configure ZFS storage pool
  hosts: homelab
  tags:
    - holab
    - storage
    - zfs
  gather_facts: true
  roles:
    - role: storage
