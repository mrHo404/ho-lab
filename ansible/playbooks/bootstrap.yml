---
# Bootstrap: common packages + Docker engine
- name: Bootstrap ho-lab host
  hosts: homelab
  gather_facts: true

  roles:
    - role: common
      tags: [common, bootstrap]
    - role: docker
      tags: [docker, bootstrap]
