---
# Uptime Kuma + Prometheus + Grafana

- name: Deploy monitoring stack
  hosts: homelab
  tags:
    - holab
    - monitoring
  gather_facts: true
  roles:
    - role: monitoring
      when: services.monitoring | default(false)
